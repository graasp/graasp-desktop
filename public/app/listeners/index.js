const loadSpace = require('./loadSpace');
const saveSpace = require('./saveSpace');
const getSpace = require('./getSpace');
const getAllSpaces = require('./getAllSpaces');

module.exports = {
  loadSpace,
  saveSpace,
  getSpace,
  getAllSpaces,
};
